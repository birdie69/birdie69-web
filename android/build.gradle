// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.7.2'
        classpath 'com.google.gms:google-services:4.4.2'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

apply from: "variables.gradle"

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

// Kotlin 1.8+ merged jdk7/jdk8 extensions into kotlin-stdlib. Some transitives
// (e.g. older Cordova) still pull kotlin-stdlib-jdk7/jdk8 1.6.x → duplicate classes.
subprojects { subproject ->
    subproject.configurations.configureEach { configuration ->
        configuration.exclude group: "org.jetbrains.kotlin", module: "kotlin-stdlib-jdk7"
        configuration.exclude group: "org.jetbrains.kotlin", module: "kotlin-stdlib-jdk8"
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
